let qualquer = "Legal";

console.log(qualquer);
console.log(typeof qualquer);

qualquer = 43;

console.log(qualquer);
console.log(typeof qualquer);

/*A tipagem fraca significa poder atribuir 
 *mais de uma classe de dados na mesma
 *variável. 
 *Por exemplo uma string e depois um número.
 */

//Evitar nomes genéricos e siglas nos identificadores das variáveis.

let valor = '';
let numero = 1; //nomes genéricos
let pqp = false; //Produto Quimico Perigoso
                 //Evitar siglas